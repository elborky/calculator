# Claude Code — local overrides
.claude/settings.local.json
# Personal session memory
.remember/
# Node / test tooling
node_modules/
test-tooling/node_modules/
# Vite static build output (artifact, not source)
dist/
# OS
.DS_Store
# Environment (sensitive)
.env
.env.local
.env.*.local
# Playwright MCP scratch output
.playwright-mcp/
