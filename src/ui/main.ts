// M2 Calculator UI — application entry point.
//
// Structural stub (T-107). Wiring lands across later groups:
//   - Group 4: M1 import + held EngineState cell + dispatch()
//   - Group 6: render(state) into #app
//   - Group 7/8: click + keyboard binding
// For now this module exists only so index.html's <script type="module"> resolves
// without error and the Vite dev server / build pipeline has a real entry.

export {}
