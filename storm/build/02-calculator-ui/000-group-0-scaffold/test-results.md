# Group 0 — Vite scaffold — verify results

Environment: node v25.9.0, npm 11.12.1. vite@8.0.16 confirmed on npm registry.

| Task | Done-when check | Result |
|---|---|---|
| T-101 | vite@8.0.16 in devDependencies, pinned exact, lock updated | PASS — `devDependencies.vite = "8.0.16"` (no caret) |
| T-102 | `dev` script resolves to vite | PASS |
| T-103 | `build` runs typecheck before bundling | PASS — `tsc --noEmit && vite build` |
| T-104 | `preview` defined | PASS — `vite preview` |
| T-105 | vite.config.ts discovered, emits to dist/ | PASS (verified via T-108 build) |
| T-106 | index.html has #app mount + module script | PASS |
| T-107 | main.ts stub exists, tsc --noEmit passes | PASS — `tsc --noEmit` exit 0 |
| T-108 | `npm run build` emits static dist/, exit 0 | PASS — `dist/index.html` + `dist/assets/index-*.js`, exit 0, built in 19ms |

Build output (T-108):
```
dist/index.html                0.32 kB │ gzip: 0.22 kB
dist/assets/index-Dezn_h7o.js  0.69 kB │ gzip: 0.39 kB
```

`dist/` added to .gitignore (build artifact, regenerated by `npm run build`).

M1 untouched: src/{engine,types,decimal-config}.ts + engine.test.ts unmodified.
Tier note: ran inline on opus (forked sonnet dispatch blocked, #FF-008) per owner decision.
