---
storm-phase: ship
storm-canonical: true
storm-depends-on:
  - storm/ship/01-security-audit.md
  - storm/ship/02-qa-report.md
  - storm/ship/03-smoke-test-plan.md
  - storm/ship/05-runbook.md
  - storm/review/04-review-theming.md
---

# SHIP Delta — v1.1.0 (M4 Theming)

> Incremental redeploy over v1.0.0. The full v1.0.0 ship ceremony (security audit, QA,
> smoke plan, runbook) is **reused** — this delta records only what changed and why a fresh
> full ceremony was right-sized away (CP-13 YAGNI / CP-7 anti-theater). Prod truth lives in
> Dokploy; this is STORM's claim of what shipped.

## What changed vs v1.0.0
- **Added:** M4 Light/Dark theme toggle (client-only). Frozen dark v3 baseline unchanged;
  new `[data-theme="light"]` "Iridescent Dawn" override + toggle control + resolution/persist
  module (`src/ui/theme.ts`) + no-FOUC inline head script.
- **Dependencies:** 0 new (vanilla `matchMedia` + `localStorage` + CSS attribute selector).
- **Schema/migrations:** none (static app — migration gate N/A).

## Delta verification (why no fresh full audit)
- **Security:** `npm audit` → 0 vulnerabilities. Surface is client-only; L8 opus audit
  (`0a0558b`) already CLEAR — no `innerHTML`/string-to-DOM (SVG icons via `createElementNS`),
  closed-set theme validation rejects hostile stored values before they reach DOM/CSS,
  secrets-clean. v1.0.0 `01-security-audit.md` posture unchanged. → CLEAR.
- **QA:** REVIEW M4 8-layer PASS (`storm/review/04-review-theming.md`), 106/106 tests, tsc 0,
  build clean. v1.0.0 `02-qa-report.md` acceptance areas unaffected. → CLEAR.
- **Smoke:** `03-smoke-test-plan.md` flows for M1–M3 unchanged; M4 toggle flow verified in
  REVIEW on the identical build (same asset hashes).

## Deploy
- **Mechanism:** push `main@dd13281` → GitHub `elborky/calculator` → Dokploy
  `application.deploy` (REST API, app `calculator-web` / `QEWTgwFSlzD5xFwXYhjKC`), Dockerfile
  multi-stage `node → nginx` build, Traefik + Let's Encrypt.
- **Prod SHA:** `dd13281`. **REVIEW-exit (staging) SHA:** `f772fa7`.

## Post-deploy smoke (automated, PASS)
- `GET https://calc.elborky.my.id/` → HTTP 200, HTTP/2.
- Prod HTML serves M4 markers: `toggle-slot`, `calc-theme` no-FOUC script, `prefers-color-scheme`,
  `"Switch to light theme"` (absent in v1.0.0 → proves v1.1.0 live).
- Assets: `index-8zsskZrx.js` (200, 42545 B) + `index-YCTcM2l1.css` (200, 14359 B) —
  byte-identical hashes to the REVIEW-verified local build.
- Security headers intact: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy.
- **Owner visual confirm (human-led, L4-02):** open the site, click the sun/moon icon — theme
  flips + persists on reload. (Code is byte-identical to the REVIEW-verified build.)

## Open follow-ups (non-blocking, parked)
- `#001` — unprefixed `backdrop-filter` → Firefox glass gap.
- `#002` — dead reduced-motion CSS selectors in `tokens.css` (L8 F-1).
- Enable HSTS in `security-headers.conf` (HTTPS confirmed live, safe to activate) → redeploy.

**Verdict: v1.1.0 SHIPPED — LIVE.**
