# M4 Theming — Autonomous Decisions Log

> Per protocol L2-05. AI-autonomous technical decisions (CP-7) made during M4 SPECIFY, with
> rationale + verification/cross-cite. Business/taste decisions live in `_briefing.md` (TD-1).

| ID | By | Decision | Rationale | Verification / cross-cite |
|---|---|---|---|---|
| **D-M4-TC-01** | sub-agent (06-tech-choices, opus) | **Swap via `[data-theme="light"]` CSS attribute-selector override** over a frozen `:root` dark default; runtime sets `document.documentElement.dataset.theme`. | Lowest-bespoke, zero-dep (#FF-004). Component CSS already reads `var(--token)` from one `:root` block (CF-3/UR-029), so overriding token values under the attribute re-skins the whole app with no component edits and leaves the shipped dark baseline untouched (CF-1). Attribute chosen over class for 1:1 fit with the data-model's closed `"light"\|"dark"` value domain. | core CSS custom properties (Baseline widely-available); head-to-head table in `06-tech-choices §1`. |
| **D-M4-TC-02** | sub-agent (06-tech-choices, opus) | **OS detection = native `window.matchMedia('(prefers-color-scheme: dark)')`** with `addEventListener('change')` (not deprecated `addListener`). No library. | Native platform signal; zero dep. | per WebFetch MDN `prefers-color-scheme` as of 2026-06 — Baseline, not deprecated; `matchMedia` + `addEventListener('change')` is the current recommended API. |
| **D-M4-TC-03** | sub-agent (06-tech-choices, opus) | **Persistence = native `localStorage` single key `calc-theme`, all access try/catch-wrapped**; no JSON, no library. Throw/blocked → treat as no-preference, fall through to OS-default. | Single 2-literal string needs no serialization; try/catch is the only correctness requirement (Safari private mode / disabled storage throw on access). Zero dep. | per WebFetch joshwcomeau dark-mode (current) as of 2026-06 — localStorage + matchMedia fallback is the still-current standard pattern. |
| **D-M4-TC-04** | sub-agent (06-tech-choices, opus) | **No-FOUC = synchronous NON-module inline `<script>` in `index.html` `<head>`**, sets `data-theme` before first paint. MUST stay plain (no `type="module"`, no external import). | A plain inline script is render-blocking + runs in document order → attribute set before paint → no flash. A `type=module` script is implicitly deferred (runs after first paint) → would reintroduce flash. Verified Vite preserves plain inline scripts verbatim (only `type=module` is bundled/transformed/deferred), so no `vite.config.ts` change needed. Zero dep. | per Context7 `/vitejs/vite` guide + MDN script-loading semantics as of 2026-06 — Vite resolves only `type=module` scripts; plain inline script preserved as-is in document order ahead of the deferred module bundle. |
| **D-M4-TC-05** | sub-agent (06-tech-choices, opus) | **Net dependency delta = 0.** No runtime/dev dependency added to `package.json`. | Entire feature = 1 CSS override block + 1 DOM attribute + matchMedia + localStorage + 1 inline script — all web-platform primitives already present. A theming lib would lose on both #FF-004 axes (adds a dep AND more wiring). | `package.json` verified this session (`dependencies: {decimal.js}` only); `06-tech-choices §0`. |
